{% if collection.handle == 'premium-bone-broth' %}

        <!--div class="alert shipping">Due to high volumes please expect shipping delays</div-->
        <div class="span12">
          <h1>Buy Premium Bone Broths Online</h1>
        </div>
        <div id="collection">

          {% paginate collection.products by settings.pagination_limit %}
          <!-- Begin collection info -->
          <div class="row">     

            <!-- Standard Recipe -->
            <!-- Start for loop to go through collections -->
            {% for collection in collections %}
              {% if collection.title == 'Premium Bone Broth' %}

              <!-- End sort collection -->
              <!-- Begin collection description -->
              <div class="span12">
                {% if collection.description != "" %}
                  {{ collection.description }}
                {% endif %}
              </div>
              <!-- End collection description -->


              <!-- End collection info -->

              <!-- Begin no products -->
              {% if collection.products.size == 0 %}
              <div class="row">
                <div class="span12 expanded-message">
                  <p>No products found</p>
                </div>
              </div>
              {% endif %}

              <div class="row products">
                {% for product in collection.products %}
{% comment %} BOLD-PRODUCT LIGHT VERSION {% endcomment %}
{% include 'bold-product' with product, hide_action: 'skip' %}

                {% include 'product-loop' with collection.handle %}
                {% endfor %}
              </div>
              {% endif %}

            {% endfor %}
            <!-- End Standard Flavor -->
            
            <!-- Combos and Deals -->
            {% for collection in collections %}
              {% if collection.title == 'Combos and Deals' %}

              <!-- End sort collection -->
              <div class="span12">
                <h2 class="collection-title w-line-middle p0"><span>{{ collection.title }}</span></h2>
                <p class="intro">You pick any combination of our premium bone broth flavors.</p>
                <p class="intro p20">Looking to get on a monthly bone broth regimen? Check out our <a href="/pages/subscriptions">Subscriptions Page</a> to save up to 20%!</p>
              </div>
              <!-- Begin collection description -->
              <div class="span12">
                {% if collection.description != "" %}
                  {{ collection.description }}
                {% endif %}
              </div>
              <!-- End collection description -->
              <!-- End collection info -->
              
              <!-- Begin no products -->
              {% if collection.products.size == 0 %}
              <div class="row">
                <div class="span12 expanded-message">
                  <p>No products found</p>
                </div>
              </div>
              {% endif %}

              <div class="row products">
                {% for product in collection.products %}
{% comment %} BOLD-PRODUCT LIGHT VERSION {% endcomment %}
{% include 'bold-product' with product, hide_action: 'skip' %}

                {% include 'product-loop' with collection.handle %}
                {% endfor %}
              </div>
              {% endif %}

            {% endfor %}
            <!-- End Combos and Deals -->
            
            <!-- End sort collection -->
              <div class="span12">
                <h2 class="collection-title w-line-middle p0"><span>Subcriptions</span></h2>
                <p class="intro p20">Sign up for a three, six, or twelve month subscription and save up to 20%!</p>
                <a href="/pages/subscriptions" class="btn">Start Saving!</a>
              </div>

            <!-- Merchandise -->
            {% for collection in collections %}
              {% if collection.title == 'Merchandise' %}

              <!-- End sort collection -->
              <div class="span12">
                <h2 class="collection-title w-line-middle"><span>{{ collection.title }}</span></h2>
              </div>
              <!-- Begin collection description -->
              <div class="span12">
                {% if collection.description != "" %}
                  {{ collection.description }}
                {% endif %}
              </div>
              <!-- End collection description -->


              <!-- End collection info -->

              <!-- Begin no products -->
              {% if collection.products.size == 0 %}
              <div class="row">
                <div class="span12 expanded-message">
                  <p>No products found</p>
                </div>
              </div>
              {% endif %}

              <div class="row products">
                {% for product in collection.products %}
{% comment %} BOLD-PRODUCT LIGHT VERSION {% endcomment %}
{% include 'bold-product' with product, hide_action: 'skip' %}

                {% include 'product-loop' with collection.handle %}
                {% endfor %}
              </div>
              {% endif %}

            {% endfor %} 
            <!-- End Merchandise -->

          {% include 'pagination' %}

          {% endpaginate %}
          <!-- End no products -->
          </div>

        </div>

        <script>
        /*
          Shopify.queryParams = {};
          if (location.search.length) {
            for (var aKeyValue, i = 0, aCouples = location.search.substr(1).split('&'); i < aCouples.length; i++) {
              aKeyValue = aCouples[i].split('=');
              if (aKeyValue.length > 1) {
                Shopify.queryParams[decodeURIComponent(aKeyValue[0])] = decodeURIComponent(aKeyValue[1]);
              }
            }
          }
          var collFilters = jQuery('.coll-filter');
          collFilters.change(function() {
              var newTags = [];
              var newURL = '';
              delete Shopify.queryParams.page;
              collFilters.each(function() { 
                if (jQuery(this).val()) {
                  newTags.push(jQuery(this).val());
                }
              });
              {% if collection.handle %}
              newURL = '/collections/' + '{{ collection.handle }}';
              if (newTags.length) {
                newURL += '/' + newTags.join('+');
              }
              var search = jQuery.param(Shopify.queryParams);
              if (search.length) {
                newURL += '?' + search;
              }
              location.href = newURL;    
              {% else %}
              if (newTags.length) {
                Shopify.queryParams.constraint = newTags.join('+');        
              }
              else {
                delete Shopify.queryParams.constraint;
              }
              location.search = jQuery.param(Shopify.queryParams).replace(/\+/g, '%20');
              {% endif %}      
          });
          jQuery('.sort-by')
            .val('{{ collection.sort_by | default: collection.default_sort_by  }}')
            .bind('change', function() {
              Shopify.queryParams.sort_by = jQuery(this).val();
              location.search = jQuery.param(Shopify.queryParams).replace(/\+/g, '%20');
            });
        */
        </script>




        <!-- Any collection besides /collections/all -->
  
        {% else %}

        <div id="collection">

          <!-- Begin collection info -->
          <div class="row p5">
            <div class="span12">
    
              <!-- Begin breadcrumb -->
              <div class="breadcrumb clearfix">
                <span itemscope itemtype="http://data-vocabulary.org/Breadcrumb"><a href="/collections/premium-bone-broth" title="Shop - All Erie Bone Broth Products" itemprop="url"><span itemprop="title">Shop</span></a></span> 
                <span class="arrow-space">&#62;</span> 
                <strong>{{ collection.title }}</strong>         

              </div>
              <!-- End breadcrumb -->
              </div>
              <div class="span12">  
              <!-- End sort collection -->
                <h1 class="collection-title p5">{{ collection.title }}</h1>
              </div>
              <div class="span12">  
                <!-- Begin collection description -->
                {% if collection.description != "" %}
                  {{ collection.description }}
                {% endif %}
                <!-- End collection description -->
              </div>
    
          </div>
          <!-- End collection info -->

          <!-- Begin no products -->
          {% if collection.products.size == 0 %}
          <div class="row">
            <div class="span12 expanded-message">
              <p>No products found</p>
            </div>
          </div>
          {% endif %}

          <div class="row products">
            {% for product in collection.products %}
{% comment %} BOLD-PRODUCT LIGHT VERSION {% endcomment %}
{% include 'bold-product' with product, hide_action: 'skip' %}

            {% include 'product-loop' with collection.handle %}
            {% endfor %}
          </div>

          <!-- End no products -->
          <div class="span12">
            <a href="/collections/premium-bone-broth" class="btn">View All Products</a>
          </div>

        </div>

        <script>
        /*
          Shopify.queryParams = {};
          if (location.search.length) {
            for (var aKeyValue, i = 0, aCouples = location.search.substr(1).split('&'); i < aCouples.length; i++) {
              aKeyValue = aCouples[i].split('=');
              if (aKeyValue.length > 1) {
                Shopify.queryParams[decodeURIComponent(aKeyValue[0])] = decodeURIComponent(aKeyValue[1]);
              }
            }
          }
          var collFilters = jQuery('.coll-filter');
          collFilters.change(function() {
              var newTags = [];
              var newURL = '';
              delete Shopify.queryParams.page;
              collFilters.each(function() { 
                if (jQuery(this).val()) {
                  newTags.push(jQuery(this).val());
                }
              });
              {% if collection.handle %}
              newURL = '/collections/' + '{{ collection.handle }}';
              if (newTags.length) {
                newURL += '/' + newTags.join('+');
              }
              var search = jQuery.param(Shopify.queryParams);
              if (search.length) {
                newURL += '?' + search;
              }
              location.href = newURL;    
              {% else %}
              if (newTags.length) {
                Shopify.queryParams.constraint = newTags.join('+');        
              }
              else {
                delete Shopify.queryParams.constraint;
              }
              location.search = jQuery.param(Shopify.queryParams).replace(/\+/g, '%20');
              {% endif %}      
          });
          jQuery('.sort-by')
            .val('{{ collection.sort_by | default: collection.default_sort_by  }}')
            .bind('change', function() {
              Shopify.queryParams.sort_by = jQuery(this).val();
              location.search = jQuery.param(Shopify.queryParams).replace(/\+/g, '%20');
            });
        */
        </script>
      {% endif %}
